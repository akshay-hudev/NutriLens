# Experiments

This folder provides lightweight scripts that wrap the evaluation pipeline.

- `run_experiment.py` runs the evaluation tooling on stored runs.
- `compare_baselines.py` merges multiple evaluation summaries for comparison.
- `ablation.py` aggregates per-run error by view count.
